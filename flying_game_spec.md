# 전설의 날개 대모험 - 개발 스펙 (바이브 코딩용)

> 이 문서는 AI 코딩 툴에 그대로 붙여넣어 프롬프트로 쓸 수 있도록 구조화했습니다.
> `[제안값]` 표시는 원문에 "상식 선에서 제안"으로 위임된 부분에 대해 합리적인 기본값을 제시한 것으로, 확정이 아니라 밸런싱 조정 대상입니다.

---

## 1. 게임 개요

- **제목**: 전설의 날개 대모험
- **장르**: 마우스 조작 비행 슈팅/러너 게임
- **그래픽 스타일**: 2D
- **핵심 루프**: 하늘을 날며 코인·돈을 모으고, 장애물을 피하고, 몬스터를 처치한다.
- **씬 구성**: `방(Room)` → 밖으로 나가면 `상점(Shop)`, 방에서 비행 시작 시 `비행 스테이지(Flight Stage)`로 이동. 세 씬을 순환.

## 2. 조작

| 입력 | 동작 |
|---|---|
| 마우스 이동 | 캐릭터가 마우스 위치를 부드럽게 보간(lerp)하며 추적 |
| Space Bar | 칼 휘두르기 (전투 중) |
| 숫자 키 1 | 아이템 슬롯 1 사용 (복이 온다 복숭아) |
| 숫자 키 2 | 아이템 슬롯 2 사용 (반짝반짝 보석) |
| 숫자 키 3 | 아이템 슬롯 3 사용 (원숭이는 바나나를 좋아해) → **게임 일시정지 + 날개 선택 UI 오픈**, 선택 완료 시 재개 |
| 숫자 키 4 | 아이템 슬롯 4 사용 (마사지 의자) |

- 이동: 마우스 좌표를 목표점으로 삼아 lerp(예: `pos += (target - pos) * 0.15`)로 부드럽게 추적. 계수는 `[제안값]`, 실제 조작감 보고 튜닝.
- 아이템 키는 소지 수량이 있을 때만 동작. 0개일 때 입력 시 무시(또는 UI 흔들림 피드백) 처리 권장.

## 3. 비행 스테이지 - 화면 요소 및 난이도 (제안값)

화면에 랜덤 스폰:
- 코인 (Coin)
- 돈 (Money) — 코인과 별개 재화
- 장애물 (Obstacle)
- 몬스터 (Monster)

**스폰 규칙 제안 (밸런싱 조정 대상)**
| 항목 | 제안값 |
|---|---|
| 코인 스폰 주기 | 1.5~2초마다 1개 |
| 돈 스폰 주기 | 4~5초마다 1개 |
| 장애물 스폰 주기 | 3초마다 1개, 화면 랜덤 위치 |
| 몬스터 등장 주기 | 15~20초마다 1마리 (1턴=60초 기준 3~4회 조우) |
| 난이도 곡선 | 턴이 진행될수록(또는 코인 누적 개수 기준) 장애물 스폰 주기를 5%씩 단축, 최소 주기 하한 설정 |

## 4. 핵심 게임 규칙

- **목숨(Life)**: 시작 3개
  - 장애물 충돌 시 -0.5
  - 목숨 0 이하가 되면 Game Over → 처음(방)으로 리셋
- **코인**: 10개 모을 때마다 아이템 1개 획득, **아이템 종류는 랜덤** (4종 중 균등 확률 `[제안값]`)
- **턴 구조**:
  - 비행 스테이지 1턴 = 1분 (타이머)
  - 1턴 종료 시 자동으로 방(Room)으로 이동, 5분간 휴식 (**휴식 타이머는 스킵 가능** — 스킵 버튼 UI 필요)
  - 게임 최초 시작 시에도 방에서 시작

## 5. 몬스터 조우 (전투) 시스템

1. 비행 중 몬스터 등장 → 화면에 **거대 철창(Cage)** 연출
2. 캐릭터와 몬스터 중 하나가 죽어야 철창에서 나갈 수 있음 (전투 강제)
3. 캐릭터가 죽으면:
   - 목숨 -1, 보석 -1
   - 즉시 부활 → 같은 몬스터와 재전투 (재전투 횟수 제한 없음, **목숨이 0이 되면 그 시점에 Game Over**)
4. 전투 조작: Space Bar = 칼 공격
5. **몬스터 스펙 제안 (밸런싱 조정 대상)**

| 항목 | 제안값 |
|---|---|
| 몬스터 체력 | 공격 3회 적중 시 처치 |
| 캐릭터 공격 쿨다운 | Space Bar 입력 후 0.4초간 재입력 불가 |
| 몬스터 공격 패턴 | 2초 주기로 캐릭터에게 접근 → 접촉 시 캐릭터 사망 처리(목숨/보석 차감 후 재전투) |
| 전투 제한시간 | 없음 (승부가 날 때까지 지속) — 단, 너무 길어지면 지루할 수 있어 30초 초과 시 몬스터 공격 빈도 증가 등 페이스업 고려 |

## 6. 방(Room) 시스템

### 6-1. 게임 최초 시작 시 (Room 최초 진입)

- **초기 보유량**: 코인 2 / 돈 2 / 보석 2

1. 팝업: "음료수를 드시겠습니까?" (예/아니오)
2. **예** 선택 시: 냉장고 음료 1개 소비 → 보석함에 보석 +1
3. **아니오** 선택 시: 바로 **날개 선택** UI로 이동
4. (예/아니오 공통) 날개 선택 UI에서 보유 날개 중 택1 후 방에 진입 완료

### 6-2. 방 안의 오브젝트

| 오브젝트 | 기능 |
|---|---|
| 보석함 (Gem Box) | 보유 보석 표시/보관. 20개 모으면(종류 무관) 트로피 1개 획득 |
| 트로피 전시대 | 보석함 뒤편 위치. 획득한 트로피 진열 |
| 냉장고 (Fridge) | 음료 보관 및 섭취 |
| 날개 걸이 (Wing Rack) | 보유 날개 진열, 방에서 장착 날개 교체 가능 |

### 6-3. 보상 체인

```
보석 20개 수집 (종류 무관, 단순 누적) → 트로피 1개 지급 → 보석 카운트 0으로 초기화
트로피 10개 수집 → 새 날개 1개와 교환 → 트로피 카운트 0으로 초기화
```

- 즉, 보석/트로피 모두 **교환 시점마다 소진(리셋)되는 재화**로 구현 (영구 누적 아님)

### 6-4. 방 밖 = 상점
- 돈 10개 → 보석 1개로 교환

### 6-5. 우편함 (상점 내)

- **트리거**: 비행 스테이지 1턴 종료 후, 상점에서 "우편" 아이콘/오브젝트 클릭
- **동작**: 편지 1통을 랜덤으로 뽑아 편지지 UI로 표시
- **효과형**: 편지지에 "편지 내용 + 발생한 효과"가 함께 표시, 클릭 즉시 스탯 반영
- **이벤트형**: 편지지에는 "편지 내용"만 표시, 실제 효과는 **방으로 돌아간 뒤** 발생

#### 6-5-1. 편지 목록

| 편지 내용 | 종류 | 효과 / 이벤트 상세 |
|---|---|---|
| 집을 나간 사이, 몬스터가 들어와서 보석을 먹었습니다. | 효과형 | 보석 -1 |
| 보석 하나가 곧 없어질 것입니다. 어서 보석 상자 안으로 숨으세요! | 이벤트형 | 방 진입 후 **5초 제한시간**. 방 왼쪽 아래 보석 상자 클릭 시 "숨을까요?" 선택지(예/아니오). **예** → 상자 안에 숨어 안전. **아니오** 또는 5초 초과 → 몬스터 등장, 꿀밤 연출 + 목숨 -0.5 |
| 몬스터가 오늘 보석 광산에 갔다고 합니다. | 효과형 | 다음 턴 비행에서 몬스터를 물리치면 보석 +1 추가 지급 |
| 오늘부터 무더위가 시작됩니다. 전기세 인상 | 효과형 | 돈 -1 |
| 당신이 주문한 과자가 왔어요 | 효과형 | 돈 -1, 목숨 +0.5 |

- 편지 등장 확률/순서: **[제안값]** 5종 풀에서 매번 랜덤 추첨(직전 편지와 중복 방지) 권장
- 우편함에 편지가 없을 때(모두 소진) UI 처리: **[제안값]** 소진 시 "새 편지가 없어요" 안내 후 풀을 리셋해 재사용

## 7. 아이템 (코인 10개당 1개 획득)

| 단축키 | 이름 | 효과 |
|---|---|---|
| 1 | 복이 온다 복숭아 | 목숨 +1 |
| 2 | 반짝반짝 보석 | 보유 보석 +1 |
| 3 | 원숭이는 바나나를 좋아해 | 사용 시 게임 일시정지 → 날개 선택 UI. 선택한 날개로 60초 동안 임시 변경 후 원래 날개로 복귀. **지속시간 중 재사용 시 60초 타이머 갱신(연장)** |
| 4 | 마사지 의자 | 30초 동안 **보석 드랍률 2배**. **지속시간 중 재사용 시 30초로 리셋** |

- 3번 아이템의 일시정지는 비행 스테이지 중에만 유효 (방/상점에서는 사용 시 즉시 날개만 변경, 정지 없음) `[제안값]`
- 마사지 의자 효과는 "몬스터 처치 시 보석 드랍 확률 2배"로 해석 `[제안값]` (개수 2배가 아님)

## 8. 날개 (Wing) 종류

| 날개 | 효과 |
|---|---|
| 기본 날개 | 기본 비행 |
| 황금 날개 | 코인 획득량 2배 (코인 1개 → 2개로 카운트) |
| 구름 날개 | 높은 고도 비행 가능. 구름 속 은신 시 몬스터에게 발견되지 않음 |
| 무지개 날개 | 전투 시 무지개 방패 + 무지개 칼 장착 |
| 하늘 날개 | 날씨 조종 (해/비/구름 선택 UI 표시, 선택 시 즉시 날씨 전환). **연출 전용, 게임플레이 수치에는 영향 없음** |
| 불꽃 날개 | 전투 시 불꽃 칼(화염 속성) 장착 |
| 물 날개 | 전투 시 물 칼(수속성) 장착 |
| 전기 날개 | 전투 시 전기 칼 장착. 전기 10회 사용 시 캐릭터 파워(Power) 50% 감소, **10초 후 자동 회복** |

## 9. 상태값 정리 (구현 시 변수화 대상)

```
Player {
  lives: float               // 최대 3, 0.5 단위 감소, 시작 3
  coins: int                 // 시작 2
  money: int                 // 시작 2
  gems: int                  // 시작 2, 20 도달 시 트로피+1 후 0으로 리셋
  trophies: int               // 10 도달 시 날개 교환 후 0으로 리셋
  equippedWing: WingType
  tempWing: WingType | null   // 3번 아이템으로 임시 장착한 날개
  tempWingTimer: number       // 60초 카운트다운(재사용 시 갱신)
  ownedWings: WingType[]
  power: float                 // 전기 날개 패널티 대상, 50% 감소 후 10초 뒤 100% 복귀
  electricUseCount: int        // 10회마다 리셋
  itemInventory: { peach: int, gem: int, monkey: int, chair: int }  // 1~4번 슬롯 보유 수량
  massageChairTimer: number    // 30초 카운트다운(재사용 시 리셋)
  nextTurnGemBonus: boolean    // "몬스터가 보석 광산에 갔다" 편지 효과 플래그
}

GameState {
  scene: "Room" | "Flight" | "Shop" | "Battle" | "GameOver" | "Mail"
  turnTimer: 60s               // 비행 턴
  restTimer: 300s              // 방 휴식 (스킵 가능)
  isPaused: boolean            // 3번 아이템 사용 시 true
  pendingMailEvent: MailEventType | null   // 이벤트형 편지 → 방 복귀 시 처리
  mailPool: MailType[]         // 소진 시 리셋되는 편지 풀
}
```

## 10. 남은 결정 필요 항목 (밸런싱/디테일)

- [ ] 3장의 제안 스폰 주기/난이도 곡선 수치 실측 튜닝
- [ ] 5장 몬스터 체력·공격 패턴 수치 실측 튜닝
- [ ] 우편 등장 확률/중복 방지 로직 확정
- [ ] 3번 아이템을 방/상점에서 사용했을 때 일시정지 여부 최종 확인
- [ ] 마사지 의자 "확률 2배" 해석이 맞는지 확인 (드랍 개수 2배일 가능성 배제 확정)
- [ ] 저연령/힐링 톤 반영: 몬스터 조우(철창)·꿀밤 연출 등 위협적 요소 순화 여부 (이전 논의에서 힐링 방향 확정했으나 본 업데이트에는 반영 안 됨 — 필요 시 재검토 요청)

---

**권장 개발 순서**: ① Player/GameState 데이터 구조 → ② Room 씬(초기 보유량 2/2/2, 음료/날개 선택 분기) → ③ Flight 씬(마우스 lerp 이동 + 스폰 + 충돌) → ④ 전투 시스템 → ⑤ 아이템/날개 효과(단축키 포함) → ⑥ 상점 + 우편함 → ⑦ 보석/트로피 보상 체인(소진형)
