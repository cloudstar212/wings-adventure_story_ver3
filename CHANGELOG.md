# 수정 이력

이 프로젝트의 변경 사항을 시간순으로 기록합니다. 새로운 변경이 있을 때마다 이 파일 상단에 항목을 추가합니다.

## 2026-09-05 (2)

- **날개 획득 방식 변경(사용자 확정)**: 트로피 10개 교환 → 지역 수호자 처치로 전환.
  `MONSTERS`의 id가 곧 wingId이므로(§Ch) `applyChargeProjectileHit()`의 처치 분기에서
  `!state.ownedWings.includes(b.monsterId)`일 때만 그 날개를 바로 지급 - 보스(`b.bossId`
  있음, monsterId는 null)는 대상 제외. 더 이상 안 쓰는 `grantRandomWing()` 삭제. 트로피는
  보상 없이 10개마다 리셋되는 순수 수집 마일스톤으로 남김
- **임시 QA 테스트 버튼 3개 추가**(`DEBUG_MODE = true`, 검증 끝나면 제거 대상 - game.js 상단
  주석·index.html·style.css의 `.debug-btn` 참조): 방 "🧪 다음 스테이지"(챕터 즉시 진행),
  비행 중 "⚔️ 엘리트로 바로"(턴 타이머 무시하고 즉시 전투 진입), 전투 중 "🏆 즉시 승리"
  (현재 전투 전원 즉시 처치 - 날개지기 배리어도 무시). `DEBUG_MODE` 동안 `loseLife()`/
  `resolveMonsterHit()`이 맨 앞에서 막혀 목숨이 전혀 줄지 않음(장애물·몬스터 공격 전부 무해)
  - 방 버튼을 처음엔 기존 우측 상단 버튼 줄에 넣었다가, 버튼이 5개가 되며 HUD(코인/보석/
    트로피/목숨)와 겹치는 걸 발견해 별도 줄(`.debug-btn`, 좌측 상단)로 이동
- 브라우저로 전체 플로우 실측: cloud 지역 진입 → "엘리트로 바로" → "즉시 승리" →
  `ownedWings`에 cloud 자동 추가 → 정산 → 방 배경이 `room_2.png`(날개 2개 아이콘)로 자동
  전환까지 한 번에 확인. "다음 스테이지" 클릭 시 챕터 증가 + 스토리 컷신 정상 표시, 목숨
  무적도 직접 확인. 콘솔 에러 없음

## 2026-09-05

GAME_DESIGN_BIBLE_v1.2 기준으로 게임을 스토리 캠페인 구조로 전환하는 작업을 시작. 저장소 히스토리
분석 결과 `game.js`/`index.html`은 구버전(wings_adventure_pc) 로직 그대로인데 `assets/`는 이후
스토리 기반 신규 자산으로 통째로 교체되어 다수 경로가 깨져 있었음(방/상점 배경, 몬스터 40종,
장애물 180장 전부 404) — 이번 세션에서 코드를 새 자산 구조에 맞게 재작성.

- **방/상점 배경 경로 수정**: `index.html`이 참조하던 `assets/backgrounds/`(복수형, 존재하지 않음)를
  실제 폴더명 `assets/background/`(단수형)로 수정. 핫스팟 좌표는 그대로 유지(같은 1536x1024 기준)
- **몬스터 시스템을 지역 수호자 7종으로 전면 교체**: 구 40종 랜덤 로스터(`assets/monsters/`, 현재
  저장소엔 아예 없음)를 폐기하고 `assets/elite/elite_{wingId}.png` 7종(구름/황금/무지개/하늘/불꽃/
  물/전기)을 "그 지역의 유일한 전투 상대"로 지정. `MONSTERS` 배열 id를 wingId와 동일하게 둬
  `currentRegionMonster()`가 바로 조회하도록 함. basic 지역은 수호자가 없어 턴 종료 시 전투 자체를
  건너뛰고 바로 방으로 복귀(`startTurnEndBattle()`에서 분기)
- **`EFFECT_ASSETS`를 지역 기준으로 재작성**: cloud→슬로우필드(바람), golden→장애물패턴(모래폭풍),
  sky→장애물소환(파편), flame→장애물소환(용암), water/electric→투사체(물줄기/번개). rainbow는 매핑
  없이 pattern("hover") 기반 페인트로 자동 분류
- **19장 스토리 캠페인 구조 신설**: `CHAPTERS` 배열(장별 제목/지역/컷신 이미지 경로) 추가,
  `state.chapter`/`chapterImageShown` 저장 필드 추가. 온보딩 완료 직후와 매 비행 턴 종료 후 해당
  챕터의 `story/chapter-*.png` 컷신을 전체화면 모달(`#modal-story`)로 표시. 비행 1턴 완료 = 다음
  챕터 진행(`advanceChapter()`, 19장에서 정지)으로 임시 매핑 - 장당 실제 턴 수는 Bible §21 TBD 항목
  이라 추후 조정 대상. `currentRegionWingId()`가 이후 모든 지역 종속 시스템(수호자/장애물/배경)의
  공통 기준점
- **장애물을 지역별 10종 체계로 교체**: 구 6테마×소/중/대 180장(자산 자체가 없었음) 폐기.
  새로 업로드된 `assets/obstacles/obstacle_{wingId}.png`(지역당 5x2 그리드 시트)를 신규
  `scripts/extract_obstacles_regions.py`(테두리 flood-fill 배경 제거, 알파 이미 있는 시트는
  그대로 크롭)로 `assets/obstacles/{wingId}/{0..9}.png` 개별 아이콘으로 추출해 연동. 크기(소/중/대)
  구분은 폐기(19장 캠페인 진행 자체가 난이도 곡선을 담당) - 히트박스 고정 반지름 하나(`OBSTACLE_RADIUS`)
- 브라우저(Playwright 대신 claude-in-chrome)로 1장(basic, 전투 스킵)→2장 전환, 4장(cloud, 구름
  수호자 전투 진입), 장애물 개별 아이콘 렌더링을 직접 재현해 확인. 콘솔 에러 없음
- **지역별 패럴랙스 비행 배경 연동**: `assets/background/{regionId}_{far,mid,near}.png`(9개 지역:
  8날개+final) 3레이어를 비행 배경으로 그림. 한 장짜리 완성 일러스트라 무한 타일링 대신, 캔버스보다
  18% 크게 확대해 그 여유 범위 안에서 레이어별로 다른 속도의 사인파로 좌우 왕복시키는 방식으로
  이음매 없는 패럴랙스 구현(`drawBackgroundLayer`). 로딩 전/자산 없음 시 기존 그라디언트+절차적
  구름으로 폴백
  - **버그 발견 및 수정**: mid/near 레이어 다수가 실제로는 불투명하거나(RGB, 알파 채널 없음)
    테두리 알파가 어중간하게 남아있어(체크무늬 프리뷰 아티팩트가 실제 픽셀/알파에 구워짐) 캔버스에
    체크무늬가 그대로 나타나는 문제를 발견. 신규 `scripts/clean_background_layers.py`로 흰 배경
    합성 후 테두리 flood-fill 재적용해 해결 - **단, `extract_obstacles_v3.py`식 "부드러운(soft)"
    알파 공식을 그대로 쓰면 체크무늬의 두 색상이 서로 다른 반투명도로 남아 알파 채널 자체에
    체크무늬가 구워지는 2차 문제가 있어(실측 확인), 배경 판정 영역은 하드컷(완전 투명)으로 변경.
    같은 결함이 `extract_obstacles_regions.py`(7단계 산출물)에도 있어 동일하게 수정 후 재추출
  - 신규 `scripts/devserver_nocache.py`: 반복 자산 수정·재검증 시 브라우저/파이썬 기본 서버 캐시로
    구버전이 보이는 문제를 막기 위한 무캐시+스레드 로컬 서버(테스트 전용, 게임 코드 아님)
- **cloud/water/electric 지역 이동 방해 엔티티 추가**: `assets/effects/asset_effect/{region}_asset.png`
  (바람 소용돌이/물 소용돌이/번개 마법진)를 배경이 아니라 코인·돈·장애물과 동일한 `rt.entities`
  스크롤 개체(`type: "hazard"`)로 구현. 5~9초 간격으로 랜덤 스폰, 데미지 없이 반경(85px) 안에
  있는 동안 이동속도만 ×0.45로 늦추고(`updatePlayerMovement`), 장애물과 달리 부딪혀도 사라지지
  않으며 화면 밖으로 스크롤돼야 제거됨(픽업 판정에서 제외). 해당 3개 지역이 아니면 스폰도
  렌더링도 되지 않음(`HAZARD_REGIONS` 게이팅) - 브라우저에서 스폰/감속(9→4.05)/렌더링/지역
  게이팅 전부 직접 확인
- **하늘 날개 날씨 연출 통합 점검**: 해/비/구름 3종 모두 실제 배경 위에서 재확인(해=painted
  달과 procedural 태양 글로우가 자연스럽게 겹침, 비=빗줄기 정상, 구름=색감 틴트 정상). 점검
  과정에서 하늘 영역에 옅은 흰 반점 노이즈가 흩뿌려진 걸 추가로 발견 - `clean_background_layers.py`
  의 배경 판정 임계값(diff)이 65였는데, 실측 결과 이 체크무늬 잔여 노이즈가 diff 0~97까지
  걸쳐 있어(반면 전경 잔디/바위 안쪽은 diff 150 이상 다수) 65로는 하늘 곳곳에 자잘한 불투명
  섬이 남았음. 100으로 상향 + 테두리 미연결 작은 섬(≤300px)도 배경으로 흡수하는 이중 안전장치
  추가 후 basic 지역 기준 하늘 영역 노이즈 픽셀 6437→0 확인, 전경 그림 손상 없음(픽셀 수
  252970→250476, 가장자리 안티에일리어싱 소폭 손실 정도의 정상 범위)
- **날개지기 보스전(11장) + 천사 최종보스(18장) 구현**: `CHAPTERS[n].boss`가 있으면
  `startTurnEndBattle()`이 지역 수호자 대신 `startBossBattle()`을 호출 - 기존 `startBattle()`
  battle 객체를 그대로 재사용해 보상/렌더 파이프라인 재구현 없이 보스 전용 필드만 얹었다.
  승리 조건은 사용자 확정대로 기존과 동일한 HP 0(정화/분리 연출은 스토리 만화가 담당, 게임
  쪽엔 추가 로직 없음)
  - **날개지기**(`assets/elite/boss_wing_guardian.png`, HP ×3): 이동속도 2.2배(빠른 회피형),
    4초/3.5초 주기로 배리어 무적 페이즈를 반복(`shieldActive`, 활성 중엔 `applyChargeProjectileHit`
    에서 데미지 무효 + 파란 링 이펙트로 표시). 공격은 매 캐스트마다 50:50으로 배리어 장판(zone)
    또는 배리어 투사체(projectile) - 신규 attackKind `wingkeeper_barrier`
  - **천사**(`assets/elite/boss_angel_full.png`, HP ×5): 특수 공격을 캐스트할 때마다
    `phaseIdx`를 한 칸씩 돌려 구름(슬로우필드)→황금(장애물패턴)→무지개/하늘(페인트)→
    불꽃(장애물소환)→물→전기(투사체×2) 순으로 attackKind와 이펙트 이미지를 통째로 교체 -
    8개 날개 능력을 순차 사용하는 연출을 새 애니메이션 없이 기존 자산 재조합으로 구현
  - **자산 버그 추가 발견**: `boss_angel_full.png`/`boss_angel_body.png`가 배경 스프라이트와
    같은 문제(RGBA인데 alpha가 전부 255, 체크무늬가 RGB에 그대로 구워짐)를 갖고 있어 전투 중
    캐릭터 주위에 흰 사각 체크무늬가 보임 - `clean_background_layers.py`의 정리 함수를 그대로
    재사용해 두 파일 수정(그림 손상 없음, 8색 날개 전부 선명하게 분리되어 보임 확인)
  - 브라우저에서 두 보스 모두 실제 전투 진입·스프라이트 렌더링·배리어 무적/피격 차단·페이즈
    순환(6단계 전부)·이펙트 이미지 전환 직접 확인. 콘솔 에러 없음
- **방 배경을 보유 날개 수/엔딩 상태에 맞춰 동적 교체**: 사용자가 준비한 `room/room_{0..9}.png`
  10장 시퀀스(벽의 "나의 날개 컬렉션" 그림에 보유 날개 아이콘이 그 수만큼 미리 그려져 있음,
  9번은 8칸 전부 빈 명패 + 작은 빛나는 깃털 - Bible §11 엔딩 묘사와 정확히 일치)를 반영.
  `roomImageSrc()`가 `state.ownedWings.length`(0~8)로 room_N을 고르고, 19장 엔딩 도달
  (`hasReachedEnding()` = 19장 도달 + 컷신 확인 완료) 이후엔 무조건 room_9으로 고정 -
  "누구도 날개를 소유하지 않는다"는 결말을 별도 게임 로직 변경 없이 배경 이미지 교체만으로
  표현(사용자 확정: 승리 조건과 마찬가지로 과한 설정 없이 처리). `renderRoom()` 진입 시마다
  갱신되어 트로피→날개 교환으로 보유 날개가 늘 때마다 바로 반영됨. 기존 `assets/background/
  room.png`(정적 단일 이미지)는 대체되어 더 이상 참조하지 않음 - 모든 room_N이 동일한
  1536x1024 레이아웃이라 기존 핫스팟 좌표(문/보석함/트로피대/냉장고) 재조정 없이 그대로
  작동하는 것까지 브라우저로 확인(4장 보유·엔딩 두 케이스 실측)

## P2 완료 - 12단계(계획 전체) 종료

1~12단계 전체 구현·검증 완료. GAME_DESIGN_BIBLE_v1.2 기준 스토리 캠페인 구조(19장) 위에
기존 게임 시스템(경제/전투/2인 협동/아이템/우편)을 그대로 얹는 작업을 마쳤다. 이번 세션에서
발견·수정한 자산 버그(체크무늬 잔상)는 배경 11장, 장애물 8세트, 보스 스프라이트 2장에 걸쳐
있었음 - 전부 `scripts/clean_background_layers.py` / `extract_obstacles_regions.py`의 테두리
flood-fill(+작은 고립 섬 제거)로 해결.

## 2026-08-23

- **비행 중 상단 재화 HUD(코인·돈·목숨) 좌우 위치 수정**: 실제로는 P1이 좌측·P2가 우측(§8-22 항목의 아이템 창 배치와 반대)이었던 것을 확인, 아이템 창과 맞춰 P1을 우측·P2를 좌측으로 이동. `.flight-hud`가 `justify-content: space-between`으로 DOM 순서에 따라 좌우가 정해지므로 `index.html`에서 두 그룹의 순서 자체를 맞바꿈(1인 모드는 P2 그룹이 숨겨져 남은 한 그룹이 항상 좌측에 오므로 영향 없음)
- **[원숭이는 바나나를 좋아해] 아이템의 임시 날개 선택 창도 캐릭터별 사진으로 표시**: `openMonkeyModal()`이 날개 선택 모달과 마찬가지로 항상 hero1 카드 일러스트만 쓰던 문제 수정. 아이템을 사용한 실제 플레이어(P1/P2)가 그 턴에 플레이 중인 캐릭터(`effectiveCharacterId()`)를 기준으로 `wingThumbSrc()`를 재사용해 썸네일을 그림
- **hero2/hero3 무지개 날개 스프라이트에서 배경에 섞여 있던 무지개 아치 제거**: `assets/sprites/{char2,char3}/rainbow.png`는 hero1과 달리 원본 디자인 시트의 해당 칸 그림 자체에 캐릭터와 무관한 하늘색 무지개 아치가 함께 그려져 있었음. `extract_new_characters.py`의 흰배경 제거는 흰색이 아닌 이 아치를 애초에 배경으로 인식하지 못해(원본 소스 아트에 포함된 내용이라 스크립트를 다시 돌려도 항상 재현됨) 지금까지 계속 남아 있었던 것으로 확인. 새 `scripts/clean_rainbow_bg.py`로 캐릭터 실루엣(hero2=명도, hero3=채도 기준) 바깥의 아치만 행 단위로 투명 처리해 제거(얼굴 좌표 박스는 별도 보호). 같은 스프라이트가 비행 중 실제 캐릭터 렌더링과 날개 선택 썸네일에 모두 쓰이므로 두 군데 다 반영됨. 캐릭터 디자인이 또 바뀌면 이 스크립트의 좌표를 재조정해야 함
- **날개 선택 썸네일(hero2/3) 배경색 재확인**: `.w-thumb`의 `background:#cfe8ff`가 `.wing-option` 하위에서 정상 적용되는 것을 현재 코드로 직접 재현 확인(스크린샷). 보고된 "배경색 없음" 증상은 재현되지 않아 캐시된 이전 페이지를 보고 있었을 가능성이 높음
- **날개 선택 썸네일(hero2/3)을 배경 있는 카드 일러스트로 교체**: 위 항목의 플랫 배경색 대신, hero1의 `assets/cards/*.png`와 똑같은 방식으로 hero2/3 원본 디자인 시트(design/캐릭터 및 날개 디자인_주인공{2,3}.png - 흰배경 아닌 버전)를 2행x4열로 잘라 `assets/cards/{char2,char3}/{wingId}.png`를 새로 만들고 `wingThumbSrc()`가 이걸 쓰도록 변경(새 `scripts/crop_hero_cards.py`). 배경이 그려진 완성 카드라 hero1과 동일하게 `object-fit:cover`로 바로 쓸 수 있어, 지난 항목에서 만든 `.w-thumb-sprite`(투명 스프라이트용 contain 처리) CSS는 더 이상 필요 없어 제거. 비행 중 실제 캐릭터 렌더링용 `assets/sprites/{char2,char3}/*.png`(무지개 아치 제거된 투명 스프라이트, 바로 위 항목)는 그대로 유지 - 용도가 다른 두 이미지라 서로 영향 없음

## 2026-08-22

- **날개 선택 창 썸네일을 캐릭터별로 표시**: 날개 선택 모달이 캐릭터와 무관하게 항상 hero1(주인공1) 카드 일러스트만 보여주던 문제 수정. hero1은 기존 `assets/cards` 일러스트를 그대로 쓰고, hero2/hero3는 해당 캐릭터가 그 날개를 장착한 실제 비행 스프라이트(`assets/sprites/{char2,char3}`)를 보여주도록 변경(`wingThumbSrc()` 신설, `activeCharacterId()` 재사용). 스프라이트는 카드 일러스트와 비율이 달라 `object-fit: cover` 대신 `contain`을 쓰는 `.w-thumb-sprite` 스타일 추가

## 2026-08-20

- **캐릭터 3종 · 2인 협동 플레이 추가**: 인원수(1/2명) 및 캐릭터(hero1~3) 선택 온보딩, 2인 모드에서 방향키+독립 공격키 조작, 몬스터 최대 2마리 동시 전투, 목숨/보석/코인/돈·아이템 인벤토리를 P1/P2 완전 분리, 2인 전용 장애물·보상 스폰 배율 적용 (SPEC.md §14~15)
  - hero2/hero3 스프라이트·차지 이펙트 자산을 전용 추출 스크립트(`scripts/extract_new_characters.py`, 기존 흰배경 제거 방식 재사용)로 생성
- **사망(dead) 상태 및 다음 방 부활 도입**: 2인 모드에서 목숨이 0이 된 플레이어는 이동/공격(진행 중이던 차지 즉시 취소)/아이템 사용/충돌 판정에서 제외되고 화면에 반투명+💀로 표시, 다른 플레이어는 전투를 계속하며 둘 다 사망해야 게임오버. 부활은 정산 후 다음 방 화면 진입 시점에 최대 목숨으로 이루어짐
- **몬스터 몸통 접촉 피해 추가**: 기존에는 몬스터 몸에 그냥 닿아도 피해가 없었는데, 장애물 충돌과 동일한 규칙(목숨 -0.5, 1초 무적, 겹쳐 있는 동안 반복 피해 없음)으로 피해를 주도록 구현
- **2인 우편함을 P1→P2 순차 처리로 변경**: 턴 종료 시 두 플레이어의 편지를 각자 독립적으로 뽑아 두고, P1 모달을 닫아야 P2 모달이 뜨는 방식으로 두 모달이 동시에 뜨지 않게 하고 개인 효과를 정확히 적용
- **아이템 단축키 재배치**: 이동키 손 위치에 맞춰 2인 모드에서 1P는 7/8/9/0, 2P는 1/2/3/4 사용(1인 모드·방/상점 화면은 기존 1/2/3/4 그대로), 비행 중 인벤토리 창도 1P 우측하단·2P 좌측하단으로 배치
- **회피/전투 화면 카메라 배율 분리**: 장애물 회피 화면(`FLIGHT_VIEW_SCALE`)만 0.65로 더 확대하고 몬스터 전투 화면(`BATTLE_VIEW_SCALE`)은 기존 0.8을 그대로 유지. 실제 브라우저 resize 이벤트가 발생해도 고정 뷰포트를 다시 측정하지 않도록 바꿔, 턴이 바뀌거나 창 크기·DPI가 바뀌어도 화면이 다시 커지던 문제를 근본적으로 제거
- **완충 차지 공격 데미지 상향**: 연타 공격 기준 페이스(dmg 10 / 쿨다운 0.45초)로 2초간 연타했을 때의 총 데미지 대비, 2초 완충 1회의 데미지가 약 1.35배가 되도록 완충 최대 데미지를 50 → 60으로 상향(0.3~2초 구간 선형 증가 구조·날개별 배율·넉백은 그대로 유지)
- 방 배경 이미지 교체

## 2026-08-18

- **전투 카메라 줌 + 뷰포트 고정**: 비행 화면 카메라를 월드 대비 80%로 축소해 캐릭터·몬스터·장애물을 더 크게 표시하고, viewport 크기를 최초 측정값으로 고정해 턴/난이도가 올라가도 화면 크기가 흔들리지 않게 함
- **차지 검기 발사 방향 고정**: 항상 플레이어 위치에서 오른쪽으로 직선 발사하도록 단순화(자동조준/유도 제거)
- **몬스터 체력 6배 상향**
- **몬스터 공격 패턴 확장**: 현재 턴 장애물 이미지를 재사용하는 공통 기본 공격 패턴 10종 추가(기존 몬스터 고유 공격 8종은 유지), 기본 공격(2~4초)과 특수 공격(6~10초) 타이머를 완전히 분리해 서로 막지 않고 각자 주기대로 발동
- **방 화면을 전체화면 레이아웃으로 변경**: 상점과 동일하게 HUD/하단 버튼을 오버레이 방식으로 전환

## 2026-08-15

- **차지 공격을 실제 투사체로 재구현**: 히트스캔에서 `rt.player.facing` 방향으로 날아가는 실제 투사체로 전면 재구현(자동조준 없음), 차징 연출을 검 스윙에서 검에 빛/펄스가 모이는 연출로 교체
- **몬스터 공격 패턴 8종 도입**: `EFFECT_ASSETS`로 몬스터-이펙트 자산 매핑을 정의하고, 몸통박치기/투사체/장애물소환/장애물패턴/미니언소환/장판/슬로우필드/페인트 8종 공격 구현(매핑이 없는 몬스터는 배회 pattern으로 페인트 vs 몸통박치기 결정)
- 위 기능에서 실제로 쓰게 된 charge/projectile/minion/thrown/zone 이펙트 자산들의 추출 결함(라벨 텍스트 혼입, 배경 반투명, 내부 흰색 삭제 등) 재추출로 수정
- SPEC.md §6(전투 시스템)을 현재 구현 기준으로 전면 재작성

## 2026-08-14

- **전투 이펙트 원본 85종 추출**(`scripts/extract_effects.py` 신설): `design/`의 이펙트 시트 5장(플레이어 차지 공격, 몬스터 투사체/투척/미니언/장판)에서 `assets/effects/{charge,projectile,minion,thrown,zone}/`로 추출 — charge 16(8종 날개×2단계) + projectile 25(5종×5티어) + minion 10(2종×5티어) + thrown 25(5종×5티어) + zone 9 = 총 85장
  - 흰 배경 시트(minion/thrown/zone)는 기존 장애물/몬스터 추출과 동일한 테두리 flood-fill 방식, 어두운 배경 시트(charge/projectile, 발광 이펙트)는 밝기를 알파로 쓰는 새 방식(`dark_bg_alpha`)으로 배경 제거
  - 검수용 contact sheet(`assets/effects_contact_*.png`, git 비추적) 5장을 직접 확인해 라벨 혼입·잘림·빈 슬롯 없이 전량 정상 추출된 것을 확인
  - **아직 추출 전용 자산이며 `game.js` 전투 로직에는 연동되지 않음** — 다음 세션에서 몬스터별 공격 패턴 매핑·수치 설계와 함께 진행 예정 (SPEC.md §12 참조)

## 2026-08-13

- **몬스터 40종 전투 시스템 도입**(`scripts/extract_monsters.py` 신설): `design/`의 몬스터 시트 2장(라벨이 아이콘 위에 있는 4행×5열 레이아웃)에서 `assets/monsters/`로 40종 추출 후 `game.js`의 `MONSTERS` 배열에 등록. 턴 수 기준 5단계 누적 오픈(1~4턴 tier1 → 21턴 이후 전체 40종), 패턴별(hover/circle/zigzag/bounce/pace) 배회 움직임, 화면 우측 절반 제한 포지셔닝 구현 (SPEC.md §6-1)
- **코인 콤보 & 자석 시스템 추가**: 콤보 10 달성 시 코인 자석 발동(시간제가 아닌 "목숨이 깎일 때까지" 유지), 콤보 5/10/20/30/50/100 마일스톤마다 자석 반경 확대 및 화면 상단 콤보 배지 색상 단계 변경(SPEC.md §5-4-1, §5-4-3)
- **코인/돈 획득 파티클·사운드 피드백 추가**: 콤보 단계에 따라 강도가 커지는 파티클과 Web Audio 합성 사운드(`playPickupSound`) (SPEC.md §5-4-2)
- **미니 이벤트 시스템 추가**: 매 비행 턴마다 코인 폭풍·장애물 러시·무지개다리 중 1~2개를 랜덤 시점에 예약 발동 (SPEC.md §5-5)
- **턴 정산 화면 추가**: 몬스터 처치로 턴이 끝날 때 코인 20개 단위 보석 자동 교환 연출(`#modal-settlement`)이 방 복귀 전에 항상 표시되도록 변경 (SPEC.md §6-2)
- **캐릭터+날개 스프라이트 재추출**(`scripts/extract_characters_v2.py` 신설): 새 원본 시트로 8종 날개 앵커 좌표(`WING_ANCHOR`)를 얼굴(눈) 중심 자동 검출 방식으로 재계산
- **장애물 추출 파이프라인 v3로 교체**(`scripts/extract_obstacles_v3.py` 신설): 기존 set1/set2 스크립트를 6개 테마별 원본 시트 기반으로 재작성(행 밀도 자동 검출 + 연결요소 클러스터링), 이웃 침범/잘림 문제를 근본적으로 개선
- 초기 컨셉 원본·구버전 장애물/캐릭터 시트를 저장소 루트에서 제거하고 `design/`(git 비추적)로 정리

## 2026-08-11 (8)

- **장애물 왼쪽 잘림 버그 수정**: 각 행의 첫 번째(가장 왼쪽) 아이템에 라벨 박스 모서리의 작은 파편이 섞여 들어가던 문제 발견 및 수정 — 세로 10px 미만의 얇은 조각은 테두리/모서리 노이즈로 간주해 연결요소 분석에서 제외
- **장애물 화질 근본 원인 해결**: 크기 확대 이후에도 "대" 사이즈가 흐려 보이는 문제의 진짜 원인은 업스케일이 아니라, 배경 제거 시 사용한 이분법(0 또는 255) 알파로 인해 테두리가 계단처럼 거칠게 잘려 있던 것이었음
  - 흰 배경 대비 부드러운(soft) 알파로 재계산해 원본의 안티에일리어싱을 최대한 보존 (색상 보정을 곁들인 첫 시도는 불투명한 내부 색상까지 어둡게 왜곡시켜 폐기하고, 알파만 부드럽게 만드는 방식으로 확정)
  - 180개 전체를 새 알고리즘으로 재추출 + 재업스케일
- **방 화면 배지(트로피/보석/음료) 세로 여백 부족 수정**: 텍스트·이모지가 알약 모양 배경을 벗어나 겹치던 문제 — padding·line-height 확대로 해결

## 2026-08-11 (7)

- **장애물 이미지 화질 개선**: 크기 확대(22/32/50px) 이후 원본 해상도(대체로 70~140px)를 화면 표시 크기까지 실시간으로 늘리면서 (특히 고해상도/배율 디스플레이에서 2~3배까지) 흐려지는 문제 발견
  - 원인: 시트에서 잘라낸 아이콘 원본 해상도가 작아, 브라우저가 실시간으로 확대(업스케일)하며 블러 발생
  - 해결: 180개 아이콘 전체를 Lanczos 리샘플링 + 언샤프마스크로 사전에 고해상도(소 130 / 중 190 / 대 260px 기준, 이미 더 큰 경우는 그대로 유지)로 다시 저장 — 이제 브라우저는 대부분 축소(다운스케일)만 하면 되므로 훨씬 또렷하게 표시됨

## 2026-08-11 (6)

- **장애물 크기 확대**: 히트박스 반지름을 소/중/대 15/22/32px → 22/32/50px로 상향 (비주얼 크기도 함께 커짐)
- **냉장고 음료 배지 위치 수정**: 오른쪽 끝에 치우쳐 있던 것을 냉장고 상단 중앙으로 이동

## 2026-08-11 (5)

- **장애물 이미지 전면 교체**: 벡터 도형 장애물을 사용자가 업로드한 일러스트(장애물 set 1/2, 6개 테마×3크기×10종=180개)로 교체
  - 테마: 우주/바다/디저트(set1), 집 안 물건/과일/교통수단(set2)
  - 1~18턴은 테마를 3턴씩(작음→중간→큼) 순서대로 진행, 19턴 이후는 매턴 테마·크기 랜덤
  - 크기 단계에 따라 충돌판정(히트박스) 반지름도 15/22/32px로 함께 변경되어 실제 난이도 요소가 되도록 구현
  - 시트에서 개별 아이콘을 잘라내는 파이썬 파이프라인 신설 (`scripts/obstacle_lib.py`, `extract_obstacles_set1/2.py`) — 연결요소(connected components) 분석으로 이웃 아이템 침범/라벨 텍스트 혼입 문제 방지

## 2026-08-11 (4)

- **배지 위치 재조정**: 트로피 배지가 캐릭터 얼굴을 가리던 문제 수정 — 트로피/보석 배지를 각각 더 우측으로 이동 (보석 배지는 보석함 중앙에 맞춤)
- **배지 글자 두께 조정**: `font-weight: 900` → `500`으로 낮춰 가독성 개선 (진한 배경+테두리 글로우만으로 충분히 대비되어, 얇은 글씨가 오히려 더 또렷하게 보임)

## 2026-08-11 (3)

- **방 화면 배지 위치/가시성 개선**: 트로피 개수·보석 개수 배지가 날개걸이 하단부와 겹쳐 날개 그림을 가리던 문제 수정 — 트로피 전시대·보석함 각각의 하단 쪽으로 이동, 글자 크기·대비·테두리 글로우를 강화해 더 잘 보이도록 변경. 보석 배지에도 🏆과 동일하게 💎 아이콘 접두어 추가

## 2026-08-11 (2)

- **목숨 최대치 변경**: 시작 목숨은 3개(게임오버 재시작 시에도 3개)로 유지하되, 아이템(복이 온다 복숭아)·편지(과자) 등으로 늘릴 수 있는 최대치를 3 → 10으로 상향 (`MAX_LIVES` 상수 도입)
- **`flying_game_spec.md` git 추적 제외**: 별도로 관리되는 문서라 `.gitignore`에 추가하고 저장소에서 제외 (파일 자체는 로컬에 유지)

## 2026-08-11

- **아이템 숫자키 조작 추가**: 인벤토리 아이템(복숭아/보석/원숭이/의자)을 숫자키 1~4로 사용할 수 있도록 변경 (클릭도 계속 지원)
- **3번 아이템(원숭이) 사용 흐름 변경**: 사용 시 비행 진행(타이머·스폰·엔티티 이동)이 완전히 멈추고, 임시로 변신할 날개를 선택하면 다시 진행되도록 변경
- **우편 시스템 추가**: 비행 턴 종료마다 상점 우편함에 무작위 편지가 도착 (최대 1통 대기, 안 읽은 편지가 있으면 새로 생기지 않음)
  - 편지 5종: 보석 감소, 보석함 은신 경고(이벤트), 다음 비행 몬스터 처치 시 보석 보너스, 돈 감소(무더위), 돈 감소·목숨 증가(과자)
  - 이벤트형 편지를 읽고 방에 돌아가면 보석함이 붉게 점멸하며 5초 카운트다운 시작, 보석함 클릭 → "숨을까요?" 확인창, 시간 초과 시 목숨 -0.5
- **마사지 의자 아이템 효과 변경** (2차례 반복 수정):
  1. 최초: 이동속도 2배 (30초)
  2. 1차 변경: 아이템 획득 시 보석 아이템 드랍 확률 2배 (30초)
  3. 2차 변경(최종): 비행 중 획득하는 돈(💰)이 10초 동안 보석(💎)으로 대신 지급됨
- **비주얼 반영**: 마사지 의자 효과가 지속되는 동안 비행 화면에 떨어지는 돈 오브젝트가 파란 보석 이미지(글로우 포함)로 표시되도록 변경, 효과 종료 시 원래 돈 이미지로 복귀

## 2026-08-09 — 초기 구현

- 방(Room) / 상점(Shop) / 비행+전투(Flight/Battle) 3씬 순수 HTML/CSS/JS(+Canvas) 게임 최초 구현
  - 코인·돈·보석·목숨 재화, 날개 8종, 아이템 4종, 트로피/보석함 보상 체인, 몬스터 조우 전투, 턴/휴식 타이머
  - `localStorage` 자동 저장, 최초 진입 시 음료→날개 선택 플로우
- 그래픽을 이모지 렌더링에서 캔버스 벡터 드로잉(그라디언트+글로우)으로 전면 교체 — 확대 시 흐려지는 문제 해결 및 스타일 개선
- 업로드된 캐릭터+날개 8종 일러스트를 AI 배경 제거로 가공하여 실제 비행 스프라이트로 적용 (`assets/sprites`), 배경이 살아있는 원본은 날개 선택 UI 카드로 활용 (`assets/cards`)
- 업로드된 방/상점 배경 일러스트를 적용하고, 그림 위에 정확히 맞춘 투명 핫스팟 버튼으로 상호작용 구현 (`assets/backgrounds`)
- 일러스트에서 추출한 돈/코인/보석 아이콘을 HUD 및 비행 화면 오브젝트에 적용 (`assets/icons`)
- 방 화면에 "새로 시작하기" 버튼 추가 (저장 데이터 초기화)
- Git 저장소 초기화 및 최초 커밋
